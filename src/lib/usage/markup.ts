// Shared markup constant (ADR-003). Used by recordUsage + recordFirecrawlUsage
// so a policy change ripples from one line.

/** Multiplier over provider cost — applied to every customer charge. */
export const MARKUP = 0.3;
